{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "env": {
    "DATABASE_URL": "your-database-connection-string"
  },
  "build": {
    "env": {
      "NEXT_TELEMETRY_DISABLED": "1"  
    },
    "commands": [
      "npx prisma generate"  
    ]
  }
}
